# AdSense 재심사 대응 작업 기록

작업일: 2026-08-28
대상: pomyjo.com (허브) + POMYJO 서브도메인 전체

이전 거부 사유는 **"가치없는 콘텐츠"** 였다 (커밋 `1c35bc4` 기준).
그 사유를 직접 겨냥해 정리한 내역과, 아직 남은 항목을 적어 둔다.

---

## 1. 허브 (pomyjo.com)

### 없던 필수 페이지 신설

두 주소 모두 **홈페이지를 그대로 반환하는 소프트 404** 상태였다.
심사에서 먼저 확인하는 페이지 두 개가 사실상 없던 셈.

| 파일 | 내용 |
|---|---|
| `about.html` | 운영 주체(의료인 아님 명시), 콘텐츠 작성 기준 5가지, 진단 도구의 한계, 수익 구조 |
| `contact.html` | 문의 유형 6종, 오류 신고 요령, FAQ 7문항 |

### 가이드 8편 추가 (7편 → 15편)

`focus` / `chronotype` / `decision-fatigue` / `compound-interest` /
`habit-formation` / `digital-detox` / `emotional-labor` / `difficult-conversation`

작성 기준:
- 출처는 실제 확인 가능한 자료만 기재. 지어낸 인용 없음
- 수치는 원 연구가 보고한 범위대로 표기 (예: 습관 자동화 중앙값 66일, 범위 18~254일)
- 21일 법칙처럼 널리 퍼졌지만 근거가 약한 통설은 출처를 밝히고 반박
- 복리 글의 모든 계산은 파이썬으로 검산 후 게재
- 건강·심리 주제 전 편에 전문가 상담 안내 + 위기 상담 번호(109 / 1577-0199)

### 결과

- 본문 총량 약 21,000자 → **47,013자**
- 21개 페이지 전부 h1 1개 / title / canonical 일치 / AdSense 스크립트
- 내부 링크 깨짐 0건, sitemap 21건 = 실제 페이지 21개 일치

---

## 2. 서브도메인 25개

크롤러에 보이는 본문이 거의 없던 페이지들을 정리했다.

| 구간 | 이전 | 현재 |
|---|---|---|
| 거의 빈 페이지 6개 | 32~229자 | 769~870자 |
| 얇은 페이지 15개 | 451~708자 | 768~1,033자 |
| 빌드 산출물 없던 4개 | 223~542자 | 864~1,253자 |

전체 본문 **최소 768자 / 중앙값 953자** (색인 제외 사이트 1개 제외).

각 사이트에 넣은 구조: 측정 영역 → 결과 읽는 법 → **사이트별로 다른 FAQ 3개**
→ 한계 고지 → 관련 허브 가이드 링크.

> FAQ를 도구마다 다르게 쓴 이유: 같은 문답을 25개 도메인에 복사하면
> 그 자체가 중복 콘텐츠가 된다.

### 함께 고친 것

- **canonical 오류** — 로컬 빌드가 `pomyjo.com/<slug>`(존재하지 않는 경로)를
  정본으로 선언하고 있었다. 서브도메인 자기 주소로 교정.
- **free-benefit-map 스크립트 파손** — 아래 3항 참고.

---

## 3. free-benefit-map 인라인 스크립트 복구

### 증상
```js
(function(){try{var s=location.hostname.split('.')[0
  { cat: 'gov', title: '청년창업지원금', ... },   // 혜택 항목 15개
];fetch('https://api.pomyjo.com/api/analytics/pageview', ...)}catch(e){}})();
```

### 원인
혜택 항목을 추가하는 패치 스크립트가 배열 리터럴을 찾다가
`split('.')[0]` 의 대괄호에 끼워 넣었다.

### 영향
1. 스크립트 전체 파싱 실패 → **애널리틱스 비콘이 아예 발송되지 않음**
2. 해당 항목 15개는 페이지에 반영되지 않는 죽은 데이터

### 확인한 것
- 떠 있던 15개는 **전부 `BENEFITS`(53개)에 이미 들어 있는 중복**이었다.
  → 유실된 데이터 없음. 실제 피해는 애널리틱스 중단뿐.
- 이 파손이 다른 곳에도 있는지 34개 사이트 인라인 스크립트를 전수 문법 검사
  → **free-benefit-map 한 곳뿐**. 시스템 전반의 문제가 아니었다.

### 복구 후 검증
JS 에러 0건 / 애널리틱스 `POST /api/analytics/pageview` 정상 발송 /
BENEFITS 53개 유지 / 카드 112개 / 버튼 17개 (변경 전과 동일)

---

## 4. 배포 시 주의사항 (다음에 작업할 때 반드시 읽을 것)

### 로컬을 그대로 올리면 안 된다

로컬 `dist/` 에는 **배포 후 스크립트로 주입된 요소가 없다**:
- "다음에 해볼 진단" 관련 도구 위젯
- 재방문 리마인더 (`alljindan-reremind`)
- 기존 `pomyjo:about` 블록
- 일부 사이트의 AdSense 스크립트

로컬을 그대로 배포하면 이것들이 전부 사라진다.
**라이브 HTML을 본문 기준으로 삼고, 로컬은 자산(assets)만 쓰는 방식**으로 작업할 것.

### Cloudflare beacon 은 제거하고 올린다

라이브 HTML을 그대로 재배포하면 CF가 서빙 시 넣는 beacon 스크립트가
파일에 굳어져 **이중 주입**된다. 배포 전에 제거할 것.

### SPA fallback 을 실제 파일로 착각하지 말 것

없는 경로에 index를 돌려주는 사이트가 많다.
`privacy.html`, `favicon.ico`, `sw.js` 등이 200으로 응답해도 실제 파일이
아닐 수 있다. **없는 경로를 하나 찔러 fallback 지문을 먼저 확보**한 뒤
해시를 비교해 걸러낼 것.

### 로컬 소스가 라이브보다 낡은 경우가 많다

허브 저장소도 4커밋 뒤처져 있었다. 손대기 전에
**자산 해시(`/assets/index-XXXX.js`)를 라이브와 대조**해 같은 빌드인지 확인할 것.

---

## 5. 손대지 않은 것과 그 이유

| 대상 | 이유 |
|---|---|
| `trend-dashboard` | 가정연합 관련 대시보드. 이미 `noindex, nofollow` 이고 허브에서 링크 없음 → 심사 대상 링크 그래프 밖. 본문 206자로 남아 있으나 색인되지 않음 |
| `aikiugihimdulda` | 본문 1,192자로 이미 기준 이상. 로컬 배포 루트에 개발용 패치 스크립트 100여 개가 섞여 있어 그대로 올리면 내부 파일이 공개된다. **확인 결과 라이브에는 노출되지 않고 있음**(전부 fallback 응답) → 조치 불필요 |
| 3개 Vite 사이트의 재빌드 | `node_modules` 가 없어 지금 빌드하면 오늘 버전 의존성이 깔려 동작이 바뀔 위험. 대신 라이브를 미러링해 처리 |

---

## 6. 남은 작업 (사람이 해야 하는 것)

1. **AdSense 콘솔에서 재심사 신청** — 자동화 불가
2. 신청 전 **Search Console에서 sitemap(21건) 재제출** →
   심사 시점에 새 페이지가 색인돼 있을 가능성을 높인다
3. `trend-dashboard` 를 계속 둘지 판단 — 심사 대상 밖이지만
   같은 도메인에 있는 것이 마음에 걸리면 별도 도메인으로 옮기는 선택지도 있음

---

## 7. 솔직한 한계

**AdSense 통과는 보장할 수 없다.** 심사는 사람이 재량으로 판단한다.
여기서 한 일은 알려진 거부 사유를 제거한 것이고, 특히 이번 사유였던
"가치없는 콘텐츠"에 대해 없던 필수 페이지를 만들고 본문을 2배 이상 늘리고
빈 페이지를 없앴다. 그 이상은 심사자의 판단 영역이다.

## 검증에 쓴 스크립트

작업 중 사용한 검증 스크립트는 세션 스크래치패드에 있었고 저장소에는 두지 않았다.
같은 점검이 다시 필요하면 아래를 재작성하면 된다.

- 허브 21페이지 구조 검증 (h1/title/canonical/adsense/중복 제목/sitemap 대조)
- 서브도메인 본문 길이·메타·canonical·noindex 일괄 감사
- 전 사이트 인라인 스크립트 `node --check` 문법 검사
- Playwright 렌더링 확인 (앱 마운트, 가로 오버플로, JS 에러, 애널리틱스 발송)
